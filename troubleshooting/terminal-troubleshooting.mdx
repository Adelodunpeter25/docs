---
title: Terminal Troubleshooting
description: Resolve common terminal session issues.
---

## Session closes unexpectedly

- Verify host/network stability
- Use reconnect action first
- If repeated, validate credentials and server SSH config

## Input/editing feels incorrect

- Confirm shell startup config is healthy
- Confirm `TERM` is set correctly
- For local shells, restart local tab after shell config changes
